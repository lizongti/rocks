return {
    Stream = require("stream/core").Stream,
    Writable = require("stream/writable").Writable,
    Transform = require("stream/transform").Transform,
    Readable = require("stream/readable").Readable,
    PassThrough = require("stream/passthrough").PassThrough,
    Observable = require("stream/observable").Observable,
    Duplex = require("stream/duplex").Duplex
}
